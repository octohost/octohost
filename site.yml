- hosts: rackspace
  user: root
  sudo: True
  gather_facts: False
  # vars_files:
  #   - vars/defaults.yml
  #   - vars/user.yml

  roles:
    - common
    - docker
    - redis
    - nodejs
    - hipache
    - serf
    - gitreceive
